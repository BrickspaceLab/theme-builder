# Slab (Brickspace Lab)

Slab-specific block types, sample JSON, and an **essential** block reference live in **[examples/slab/README.md](slab/README.md)** (with `*.json` examples in the same folder). For a machine-generated list of every block, run `scripts/generate_slab_block_reference.py` (stdout).
